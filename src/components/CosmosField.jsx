import {useEffect,useRef} from 'react';
export function CosmosField(){const ref=useRef(null);useEffect(()=>{const c=ref.current,ctx=c.getContext('2d',{alpha:true});let w=1,h=1,dpr=1,raf=0,t=0,mx=.5,my=.5,tx=.5,ty=.5;const stars=Array.from({length:170},(_,i)=>({x:Math.random(),y:Math.random(),r:Math.random()*1.15+.15,a:Math.random()*.55+.08,z:Math.random()*1.3+.2}));const resize=()=>{dpr=Math.min(2,devicePixelRatio||1);w=innerWidth;h=innerHeight;c.width=Math.round(w*dpr);c.height=Math.round(h*dpr);c.style.width=w+'px';c.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0)};const move=e=>{tx=e.clientX/w;ty=e.clientY/h};resize();addEventListener('resize',resize);addEventListener('pointermove',move,{passive:true});let last=performance.now();const draw=now=>{const dt=Math.min(.05,(now-last)/1000);last=now;t+=dt;mx+=(tx-mx)*(1-Math.exp(-2.2*dt));my+=(ty-my)*(1-Math.exp(-2.2*dt));ctx.clearRect(0,0,w,h);const g1=ctx.createRadialGradient(w*(.68+(mx-.5)*.04),h*(.18+(my-.5)*.03),0,w*.68,h*.18,Math.max(w,h)*.52);g1.addColorStop(0,'rgba(16,91,210,.10)');g1.addColorStop(.38,'rgba(10,40,100,.045)');g1.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=g1;ctx.fillRect(0,0,w,h);for(const s of stars){const x=(s.x*w+(mx-.5)*s.z*14+w)%w;const y=(s.y*h+(my-.5)*s.z*10+h)%h;const tw=.72+.28*Math.sin(t*(.3+s.z*.18)+s.x*12);ctx.globalAlpha=s.a*tw;ctx.fillStyle=s.r>.8?'#b8dcff':'#6faeff';ctx.beginPath();ctx.arc(x,y,s.r,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;raf=requestAnimationFrame(draw)};raf=requestAnimationFrame(draw);return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);removeEventListener('pointermove',move)}},[]);return <canvas className="v5-cosmos" ref={ref} aria-hidden="true"/>}
